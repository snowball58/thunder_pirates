Current user is 
= @current_email

%table
    %thead
        %tr
            %th E-mail address
            %th Delete
    %tbody
    - @users.each do |user|
        %tr
            %td= user.email
            %td= button_to 'delete', admin_delete_user_path(:id => user.id), :method => :delete
            
%br
= link_to("Admin", auth_user_root_path)