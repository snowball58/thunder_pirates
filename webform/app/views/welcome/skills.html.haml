.main-title Skills and Special Abilities

-# The method attribute specifies how to send form data (GET or POST)
-# GET is default and appends the data to the URL in name/value pairs URL?name=value&name=value
-# POST sends data as HTTP post transaction 
-# Form data is sent to the page specified in the action attribute

=form_tag welcome_skills_path, :method => :post do
  .form-item
    %span Do you speak any languages other than english?
    = radio_button_tag(:multilingual_speaker, "yes")
    = label_tag(:multilingual_speaker_yes, "Yes")

    = radio_button_tag(:multilingual_speaker, "no")
    = label_tag(:multilingual_speaker_no, "No")
    %br
    %span If so, what language(s)?
    = text_field_tag(:speaking_languages)

  .form-item
    %span Do you read/write in any language other than English?
    = radio_button_tag(:multilingual_reader, "yes")
    = label_tag(:multilingual_reader_yes, "Yes")

    = radio_button_tag(:multilingual_reader, "no")
    = label_tag(:multilingual_reader_no, "No")
    %br
    %span If so, what language(s)?
    = text_field_tag(:reading_languages)

  .form-item
    %span Do you have any experience with blind or hearing impaired persons?
    = radio_button_tag(:impaired_experience, "yes")
    = label_tag(:impaired_experience_yes, "Yes")

    = radio_button_tag(:impaired_experience, "no")
    = label_tag(:impaired_experience_no, "No")
    %br
    %span If so, in what capacity?
    = text_area_tag(:impaired_capacity)

  .form-item
    %span Do you have any experience with handicapped persons?
    = radio_button_tag(:handicapped_experience, "yes")
    = label_tag(:handicapped_experience_yes, "Yes")

    = radio_button_tag(:handicapped_experience, "no")
    = label_tag(:handicapped_experience_no, "No")
    %br
    %span If so, in what capacity?
    = text_area_tag(:handicapped_capacity)
  
  .form-item  
    = submit_tag 'Continue', :class => "button"